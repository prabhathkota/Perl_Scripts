#!/usr/intel/bin/perl                                                                                  
use strict;
use warnings;

my @userinput = <STDIN>;

foreach my $each (@userinput) {
    print "\n Hello ... " . $each;
}

